## 0. Spike: what Claude Code reports for running work

- [ ] 0.1 Drive a real Claude Code session through `bun run dev` with a backgrounded Bash (`sleep 60 &`-style), a backgrounded Agent, a foreground Agent, and `/code-review`; capture the SDK frames (`task_started`/`task_progress`/`task_updated`/`task_notification`, `tool_progress`, and the launching tool results) and record the answers to design.md's Open Questions in `openspec/changes/fix-workspace-activity-states/spike-background-tasks.md`. Verify the file answers all three questions and D7/D8 are adjusted if they differ.

## 1. Background work counts as working (#388)

- [ ] 1.1 In `src/chat/adapter.ts`, add a `backgroundWork` set beside `liveTurns`, maintained in the projection status callback (`status === "background"` adds, any other status removes) and cleared by `forgetActivity`; make `activity().working` true when either set is non-empty; update the `ChatActivity` doc comment in `src/chat/types.ts`. Verify with new cases in `src/chat/adapter.test.ts`: a conversation entering `background` after its turn ends keeps `working: true`; `idle` after the background set clears reports `working: false` with exactly one change notification; cancel/held-prompt tests still pass (`bun test src/chat/adapter.test.ts`).
- [ ] 1.2 Mirror the rule in the e2e fixture `tests/e2e/chat-service.ts` `activity()` and verify `bun test src/server/chat-routes.test.ts` plus `tests/e2e/hub-switcher.e2e.ts` still pass.

## 2. The `finished` fact on the wire (#383)

- [ ] 2.1 Extend `WorkspaceActivity` in `src/shared/live-protocol.ts` with `finished: boolean`; `sanitizeWorkspaceActivity` treats a missing/non-true value as false and forces false when not running. Verify with `src/shared/live-protocol.test.ts` cases for present, missing, and not-running inputs.
- [ ] 2.2 In `src/hub/live-broker.ts`, add broker-level `observed`, `finishedAt`, and per-user `viewedAt` marks per design D2/D3; compose `finished` into each feed value; extend `sameActivity` to four fields; drop marks on `NOT_RUNNING`, `unavailable`, registry removal, and a new `working: true`; add `acknowledgeViewed(user, workspaceId)` that re-derives and re-emits for that user's feed. Verify with `src/hub/live-broker.test.ts`: working→idle marks finished for every user with a feed; `RUNNING_UNKNOWN`→idle does not; working→awaiting does not; acknowledge clears for one user only and emits once; stop clears; a user whose feed was dropped and re-created still sees finished; payload keys are exactly `awaiting, finished, running, working`.
- [ ] 2.3 Add the hub-handled `POST /s/<ws>/api/activity-viewed` in `src/hub/server.ts` beside `/api/personal-state` (CSRF check, 404 unknown workspace, 405 other methods, 204 on success, calls `broker.acknowledgeViewed`). Verify with a server route test (existing `src/hub/server*.test.ts` pattern) covering success, unknown workspace, and cross-origin rejection.
- [ ] 2.4 Update `src/shell/live-channel.ts` and its test if they assert or type the activity payload shape; verify `bun test src/shell/live-channel.test.ts src/hub/live-endpoint.test.ts`.

## 3. Switcher presentation and acknowledgement (#383)

- [ ] 3.1 In `src/shell/hub-nav.ts`, add the `finished` tone/text to `workspaceMenuState`, the `finished` badge kind with precedence awaiting > finished > working in `switcherBadge`, and the label `Work finished in N workspace(s)`; apply `is-finished` classes in `updateChip` and `appendWorkspace`. Verify with `src/shell/hub-nav.test.ts` cases for the menu text, badge precedence with all three present, and the label.
- [ ] 3.2 Add `.hub-menu-state.is-finished` and `.hub-activity-badge.is-finished` (accent count pill, no pulse) to `src/styles.css`; verify visually in `bun run dev` with the e2e in 6.1 or a manual check.
- [ ] 3.3 In `src/chat/ui.ts`, dispatch a `uatu:chat-surface-active` document event where `pendingNewContent` is cleared (surface became active) and once at bootstrap when already active. In `hub-nav.ts`, on that event and on each activity update for the current workspace, POST `appUrl("/api/activity-viewed")` when the current workspace reads `finished` and the surface is active (hub mode only, failures ignored). Verify with `src/shell/hub-nav.test.ts` (posts once per finished state, not when idle, not without hub).

## 4. Running tasks carry their facts (#385)

- [ ] 4.1 Extend `BackgroundTaskItem` in `src/chat/types.ts` with optional `subagentType`, `prompt`, `usage { totalTokens, toolUses, durationMs }`, `outputFile`, `childConversationId`; accept them in `src/chat/validation.ts`. Verify with `src/chat/validation.test.ts` (accepted when well-formed, stripped when malformed).
- [ ] 4.2 In `src/chat/claude/normalization.ts` `backgroundTaskUpdate`, keep `task_started.subagent_type`/`prompt`, `task_progress.usage`/`summary`, `task_notification.output_file`, and read the launching tool result's async `AgentOutput` (`agentId` → `childConversationId`, `outputFile`) in `toolResultUpdate`; derive `childConversationId` for `local_agent` tasks per the spike's task-id/agent-id finding. In `src/chat/claude/provider.ts` set `agentProgressSummaries: true` and `forwardSubagentText: true` on the query. Verify with `src/chat/claude/normalization.test.ts` and `provider.test.ts` cases for each field and for a progress summary replacing `Using <tool>`.
- [ ] 4.3 Add the shell-task output read: `ChatService.taskOutput(conversationId, taskId, { tailBytes })` → provider reads a bounded tail of the item's `outputFile`, refusing paths outside Claude Code's config directory and tasks not owned by the conversation; route `GET /api/chat/conversations/:id/tasks/:taskId/output` in `src/server/routes.ts` (capability-gated, 404 until a path is known). Verify with `src/server/chat-routes.test.ts` and a provider test using a temp file (tail bounds, path refusal, unknown task).

## 5. Inspecting running work (#385)

- [ ] 5.1 Route subagent frames to the child (design D8): the provider keeps `parentToolUseId → childConversationId` from `task_started`/the async launch result and emits `parent_tool_use_id`-tagged frames under `sub:<parentSessionId>:<agentId>`; the adapter publishes them on the child's topic via its existing `parentId` routing. Verify with `src/chat/claude/provider.test.ts` (a subagent's text and tool frames reach the child id, the parent keeps only its launching row) and `src/chat/adapter.test.ts` (child projection updates while open; the launching row's attribution is unchanged); `src/chat/timeline-renderer.test.ts` "subagent entries" still passes.
- [ ] 5.2 Make composer background-task rows and subagents-track rows buttons while running: an agent task/subagent opens `openChildConversation(childConversationId)`; the subagents track derives a running entry's `conversationId` from the task item or the launching row as soon as known and shows its progress note. Verify with `src/chat/background-tasks.test.ts`, `src/chat/timeline-renderer.test.ts`, and a `ui`-level test that a running agent row opens the drill-down.
- [ ] 5.3 Add the drill-down header for a running task (description, subagent type, progress note, elapsed from `createdAt`, tokens/tool uses, Stop) kept in sync by the existing background-task repaint, and the shell task view (command, elapsed, progress, Stop, output pane re-fetched on a short timer while running and once on settle; "output not yet available" until `outputFile` is known). Verify with unit tests for the header/view rendering and timer lifecycle (stops on close and on settle), and `src/styles.css` for the header strip.
- [ ] 5.4 Extend the e2e fixture `tests/e2e/chat-service.ts` with control actions to publish a running task with progress/usage/`childConversationId`, a live child, and a fake task output; add e2e coverage in `tests/e2e/chat-claude-polish.e2e.ts` (or a sibling): open a running agent task → child transcript updates live and shows the header; open a running shell task → output pane refreshes; Stop from the view settles the row. Verify with `bun test:e2e -- chat-claude-polish`.

## 6. End-to-end, docs, and evidence

- [ ] 6.1 Extend `tests/e2e/hub-switcher.e2e.ts`: a workspace that was working goes idle → its entry reads `finished` and the chip shows the finished badge; a background-only workspace reads `working`; opening the finished workspace with the chat in view clears the entry and badge on another open page. Capture desktop and phone screenshots with `UATU_E2E_SCREENSHOTS_DIR=openspec/changes/fix-workspace-activity-states/screenshots` at PR time. Verify with `bun test:e2e -- hub-switcher`.
- [ ] 6.2 Manually verify against real Claude Code in `bun run dev`: a `/code-review` turn lists its tasks and agents while working, a running agent opens live, the switcher shows `working` then `finished`, and viewing clears it; note results in the PR.
- [ ] 6.3 Update `ARCHITECTURE.md` prose on the activity topic (four facts, the viewed acknowledgement) and on subagent transcripts (live while running, frames routed to the child); run the full `bun test` suite and `bun run typecheck` (or the repo's equivalent); verify green.
